# FRT-6 마이페이지 (프로필 편집·플랜·계정 정보 UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `app/(main)/settings/page.tsx` 를 프로필 편집 폼·플랜·계정 정보·비밀번호 진입점이 있는 데스크톱 우선 반응형 계정 화면으로 재구성한다.

**Architecture:** `components/features/settings/` 에 4개의 독립 카드 컴포넌트를 만들고 페이지는 얇은 컴포저로 조합한다. 계정 정보는 `GET /auth/me`(useAuth) 데이터로 실제 동작하고, 백엔드 미완성 동작(프로필 저장=BAC-5, 비밀번호 재설정=FRT-8)은 노출하되 비활성 처리한다.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript strict, Tailwind v4. 기존 `@/components/ui` 프리미티브와 `app/(auth)/constants.ts` 재사용. 신규 의존성 없음.

> **테스트 정책:** 레포에 테스트 러너 없음(scripts: dev/build/lint/typecheck). 단위테스트 대신 **태스크별 `npm run typecheck` + `npm run lint`**, 마지막에 `npm run build` + 브라우저 스모크로 검증한다(spec §9). 모든 명령은 워크트리 루트 `/Users/drchasekim/Developer/arc/arc-frontend/.claude/worktrees/frt-6` 에서 실행.

---

## File Structure

| File | 책임 |
|---|---|
| `components/features/settings/AccountInfoCard.tsx` (create) | 이메일·인증 여부·연결된 소셜 표시 (live) |
| `components/features/settings/PlanCard.tsx` (create) | Free 고정 표시 + 업그레이드 안내(비활성) |
| `components/features/settings/SecurityCard.tsx` (create) | 비밀번호 변경/설정 진입점(비활성) |
| `components/features/settings/ProfileEditForm.tsx` (create) | 온보딩 항목 편집 입력 + 방어적 prefill + 저장 비활성 |
| `app/(main)/settings/page.tsx` (modify) | identity strip + 반응형 grid 조합, 로그아웃 유지 |

---

## Task 1: AccountInfoCard (live, 가장 단순)

**Files:**
- Create: `components/features/settings/AccountInfoCard.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { Card, CardHeader, CardTitle, Badge } from "@/components/ui";
import type { Account } from "@/types/auth";

interface AccountInfoCardProps {
  account: Account;
}

const OAUTH_LABELS: Record<string, string> = {
  google: "Google",
  kakao: "Kakao",
  naver: "Naver",
  apple: "Apple",
};

export function AccountInfoCard({ account }: AccountInfoCardProps) {
  const socials = account.connected_oauth ?? [];

  return (
    <Card variant="default" padding="lg">
      <CardHeader>
        <CardTitle>계정 정보</CardTitle>
      </CardHeader>
      <dl className="flex flex-col gap-4">
        <div className="flex items-center justify-between gap-4">
          <dt className="text-body-sm text-text-secondary shrink-0">이메일</dt>
          <dd className="text-body text-text-primary truncate">{account.email}</dd>
        </div>
        <div className="flex items-center justify-between gap-4">
          <dt className="text-body-sm text-text-secondary shrink-0">이메일 인증</dt>
          <dd>
            {account.email_verified ? (
              <Badge variant="success">인증됨</Badge>
            ) : (
              <Badge variant="warning">미인증</Badge>
            )}
          </dd>
        </div>
        <div className="flex items-center justify-between gap-4">
          <dt className="text-body-sm text-text-secondary shrink-0">연결된 소셜</dt>
          <dd className="text-body text-text-primary text-right">
            {socials.length > 0 ? (
              socials.map((p) => OAUTH_LABELS[p] ?? p).join(", ")
            ) : (
              <span className="text-text-tertiary">없음</span>
            )}
          </dd>
        </div>
      </dl>
    </Card>
  );
}
```

- [ ] **Step 2: 타입·린트 검증**

Run: `npm run typecheck && npm run lint`
Expected: 0 errors (새 파일 관련 에러 없음)

- [ ] **Step 3: 커밋**

```bash
git add components/features/settings/AccountInfoCard.tsx
git commit -m "✨ 마이페이지 계정 정보 카드 추가"
```

---

## Task 2: PlanCard (Free placeholder)

**Files:**
- Create: `components/features/settings/PlanCard.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { Card, CardHeader, CardTitle, Badge, Button } from "@/components/ui";

export function PlanCard() {
  return (
    <Card variant="default" padding="lg">
      <CardHeader>
        <CardTitle>현재 플랜</CardTitle>
      </CardHeader>
      <div className="flex items-center justify-between gap-4">
        <Badge variant="brand">Free</Badge>
        <span className="text-body-sm text-text-tertiary">무료 플랜 이용 중</span>
      </div>
      <p className="mt-4 text-body-sm text-text-secondary">
        더 많은 분석과 기능이 필요하신가요? Pro 업그레이드를 준비하고 있어요.
      </p>
      <Button variant="secondary" size="sm" fullWidth className="mt-3" disabled>
        Pro 업그레이드 (준비 중)
      </Button>
    </Card>
  );
}
```

- [ ] **Step 2: 타입·린트 검증**

Run: `npm run typecheck && npm run lint`
Expected: 0 errors

- [ ] **Step 3: 커밋**

```bash
git add components/features/settings/PlanCard.tsx
git commit -m "✨ 마이페이지 플랜 카드(Free placeholder) 추가"
```

---

## Task 3: SecurityCard (비밀번호 진입점, 비활성)

**Files:**
- Create: `components/features/settings/SecurityCard.tsx`

- [ ] **Step 1: 컴포넌트 작성**

```tsx
import { Card, CardHeader, CardTitle, Button } from "@/components/ui";

interface SecurityCardProps {
  hasPassword: boolean;
}

export function SecurityCard({ hasPassword }: SecurityCardProps) {
  const label = hasPassword ? "비밀번호 변경" : "비밀번호 설정";
  const hint = hasPassword
    ? "비밀번호를 변경할 수 있어요."
    : "소셜 로그인 계정이에요. 비밀번호를 설정하면 이메일로도 로그인할 수 있어요.";

  return (
    <Card variant="default" padding="lg">
      <CardHeader>
        <CardTitle>보안</CardTitle>
      </CardHeader>
      <p className="text-body-sm text-text-secondary mb-3">{hint}</p>
      {/* TODO(FRT-8): 비밀번호 재설정 화면 연결 + disabled 해제 */}
      <Button variant="secondary" size="sm" fullWidth disabled>
        {label} (준비 중)
      </Button>
    </Card>
  );
}
```

- [ ] **Step 2: 타입·린트 검증**

Run: `npm run typecheck && npm run lint`
Expected: 0 errors

- [ ] **Step 3: 커밋**

```bash
git add components/features/settings/SecurityCard.tsx
git commit -m "✨ 마이페이지 보안(비밀번호 진입점) 카드 추가"
```

---

## Task 4: ProfileEditForm (편집 입력 + 방어적 prefill + 저장 비활성)

**Files:**
- Create: `components/features/settings/ProfileEditForm.tsx`

**주의:**
- `phone` 은 raw digit 으로 보관하고 `formatPhone()` 로 표시(온보딩과 동일).
- `education` → 소속 매핑은 `AFFILIATION_OPTIONS[].value` 와 일치할 때만 선택. 불일치 시 미선택(graceful).
- 학교/학과는 소속이 student 이거나 기존 값이 있을 때 노출(데모 `education:"재학"` + 학교/학과 보유 케이스 방어).
- worry/interest 는 옵션 목록에 존재하는 값만 선택 표시.
- 저장 버튼은 항상 `disabled` (BAC-5). dirty 추적/onSave 미구현(YAGNI) — 후속 이슈에서 추가.

- [ ] **Step 1: 컴포넌트 작성**

```tsx
"use client";

import { useState } from "react";
import {
  Input,
  DatePicker,
  Chip,
  Button,
  Card,
  CardHeader,
  CardTitle,
} from "@/components/ui";
import {
  AFFILIATION_OPTIONS,
  Q1_OPTIONS,
  INTEREST_OPTIONS,
  formatPhone,
  type AffiliationStatus,
} from "@/app/(auth)/constants";
import type { Profile } from "@/types/auth";

interface ProfileEditFormProps {
  profile: Profile | null;
}

/** 읽기 포맷(education)이 AFFILIATION_OPTIONS.value 와 일치할 때만 매핑, 아니면 미선택 */
function toAffiliation(education: string | undefined): AffiliationStatus | "" {
  const match = AFFILIATION_OPTIONS.find((o) => o.value === education);
  return match ? match.value : "";
}

export function ProfileEditForm({ profile }: ProfileEditFormProps) {
  const [name, setName] = useState(profile?.name ?? "");
  const [birth, setBirth] = useState(profile?.birth ?? "");
  const [phone, setPhone] = useState((profile?.phone ?? "").replace(/\D/g, ""));
  const [affiliation, setAffiliation] = useState<AffiliationStatus | "">(
    toAffiliation(profile?.education)
  );
  const [school, setSchool] = useState(profile?.school ?? "");
  const [department, setDepartment] = useState(profile?.department ?? "");
  const [worry, setWorry] = useState<string[]>(
    (profile?.worry ?? []).filter((w) => (Q1_OPTIONS as readonly string[]).includes(w))
  );
  const [interest, setInterest] = useState<string[]>(
    (profile?.interest ?? []).filter((i) =>
      (INTEREST_OPTIONS as readonly string[]).includes(i)
    )
  );

  const showSchoolFields = affiliation === "student" || !!school || !!department;

  function toggle(list: string[], setList: (v: string[]) => void, value: string) {
    setList(list.includes(value) ? list.filter((v) => v !== value) : [...list, value]);
  }

  return (
    <Card variant="default" padding="lg">
      <CardHeader>
        <CardTitle>프로필 편집</CardTitle>
      </CardHeader>

      <div className="flex flex-col gap-5">
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <Input
            label="이름"
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="이름"
          />
          <DatePicker
            label="생년월일"
            mode="date"
            value={birth}
            onChange={(e) => setBirth(e.target.value)}
          />
          <Input
            label="전화번호"
            type="tel"
            inputMode="numeric"
            value={formatPhone(phone)}
            onChange={(e) => setPhone(e.target.value.replace(/\D/g, "").slice(0, 11))}
            placeholder="010-0000-0000"
          />
          <div className="flex flex-col gap-1.5">
            <span className="text-label text-text-primary">소속</span>
            <div className="grid grid-cols-2 sm:grid-cols-4 gap-2">
              {AFFILIATION_OPTIONS.map((opt) => {
                const active = affiliation === opt.value;
                return (
                  <button
                    key={opt.value}
                    type="button"
                    onClick={() => setAffiliation(opt.value)}
                    className={[
                      "h-12 rounded-md border text-body-sm font-semibold transition-colors",
                      active
                        ? "border-brand bg-surface-brand text-brand-dark"
                        : "border-border text-text-secondary hover:border-brand hover:text-brand",
                    ].join(" ")}
                  >
                    {opt.label}
                  </button>
                );
              })}
            </div>
          </div>
        </div>

        {showSchoolFields && (
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <Input
              label="학교"
              value={school}
              onChange={(e) => setSchool(e.target.value)}
              placeholder="학교"
            />
            <Input
              label="학과"
              value={department}
              onChange={(e) => setDepartment(e.target.value)}
              placeholder="학과"
            />
          </div>
        )}

        <div className="flex flex-col gap-2">
          <span className="text-label text-text-primary">고민</span>
          <div className="flex flex-wrap gap-2">
            {Q1_OPTIONS.map((opt) => (
              <Chip
                key={opt}
                selected={worry.includes(opt)}
                onClick={() => toggle(worry, setWorry, opt)}
              >
                {opt}
              </Chip>
            ))}
          </div>
        </div>

        <div className="flex flex-col gap-2">
          <span className="text-label text-text-primary">관심사</span>
          <div className="flex flex-wrap gap-2">
            {INTEREST_OPTIONS.map((opt) => (
              <Chip
                key={opt}
                selected={interest.includes(opt)}
                onClick={() => toggle(interest, setInterest, opt)}
              >
                {opt}
              </Chip>
            ))}
          </div>
        </div>

        <div className="flex flex-col gap-2 pt-1">
          <p className="text-body-sm text-text-tertiary">프로필 저장 기능은 곧 제공돼요.</p>
          {/* TODO(BAC-5): lib/api/auth-api.ts 에 updateProfile() 추가 후 onSave 연동 + disabled 해제 */}
          <Button variant="primary" disabled className="sm:self-start">
            저장
          </Button>
        </div>
      </div>
    </Card>
  );
}
```

- [ ] **Step 2: 타입·린트 검증**

Run: `npm run typecheck && npm run lint`
Expected: 0 errors (특히 `@/app/(auth)/constants` import 해석, unused var 없음 확인)

- [ ] **Step 3: 커밋**

```bash
git add components/features/settings/ProfileEditForm.tsx
git commit -m "✨ 마이페이지 프로필 편집 폼 추가 (저장 비활성, BAC-5 연동 전)"
```

---

## Task 5: settings 페이지 조합 (modify)

**Files:**
- Modify: `app/(main)/settings/page.tsx` (전체 교체)

- [ ] **Step 1: 페이지 재작성**

기존 파일 전체를 아래로 교체:

```tsx
"use client";

import { useAuth } from "@/hooks/useAuth";
import { api } from "@/lib/api/client";
import { Button, Card } from "@/components/ui";
import { ProfileEditForm } from "@/components/features/settings/ProfileEditForm";
import { PlanCard } from "@/components/features/settings/PlanCard";
import { AccountInfoCard } from "@/components/features/settings/AccountInfoCard";
import { SecurityCard } from "@/components/features/settings/SecurityCard";

export default function SettingsPage() {
  const { user, isLoading } = useAuth();

  async function handleLogout() {
    await api.post("/auth/logout").catch(() => {});
    window.location.href = "/login";
  }

  if (isLoading) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <p className="text-body text-text-tertiary">불러오는 중...</p>
      </div>
    );
  }

  const account = user?.account;
  const profile = user?.profile ?? null;

  return (
    <div className="mx-auto w-full max-w-4xl px-4 sm:px-6 py-10 sm:py-12">
      <h1 className="text-heading-2 text-text-primary mb-8">내 계정</h1>

      <Card variant="default" padding="lg" className="mb-6 flex items-center gap-4">
        <div className="w-14 h-14 rounded-full bg-surface-secondary flex items-center justify-center text-heading-2 text-text-tertiary shrink-0">
          {profile?.name?.[0] ?? "?"}
        </div>
        <div className="flex-1 min-w-0">
          <p className="text-title font-semibold text-text-primary truncate mb-0.5">
            {profile?.name ?? "이름 없음"}
          </p>
          <p className="text-body-sm text-text-secondary truncate">{account?.email}</p>
        </div>
      </Card>

      <div className="grid grid-cols-1 lg:grid-cols-12 gap-6">
        <div className="lg:col-span-8">
          <ProfileEditForm profile={profile} />
        </div>
        <div className="lg:col-span-4 space-y-6">
          <PlanCard />
          {account && <AccountInfoCard account={account} />}
          {account && <SecurityCard hasPassword={account.has_password} />}
          <Button variant="destructive" fullWidth onClick={handleLogout}>
            로그아웃
          </Button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 타입·린트 검증**

Run: `npm run typecheck && npm run lint`
Expected: 0 errors (unused import 없음 — Card 는 identity strip 에서 사용)

- [ ] **Step 3: 커밋**

```bash
git add "app/(main)/settings/page.tsx"
git commit -m "✨ 마이페이지 레이아웃 재구성 (프로필·플랜·계정·보안 조합)"
```

---

## Task 6: 빌드 + 브라우저 스모크 검증

**Files:** 없음(검증 전용)

- [ ] **Step 1: 프로덕션 빌드**

Run: `npm run build`
Expected: 빌드 성공, `/settings` 라우트 컴파일 에러 없음

- [ ] **Step 2: 브라우저 스모크 (Playwright MCP)**

`npm run dev` 후 로그인/데모 상태로 `/settings` 접속하여 확인:
- 데스크톱 폭(≥1024px): 좌측 프로필 폼(col-span-8) + 우측 사이드바(플랜/계정/보안/로그아웃) 2-col 레이아웃
- 모바일 폭(≤375px): 단일 컬럼 스택, 칩 wrap, 버튼 full-width
- 계정 정보: 이메일 표시, `email_verified` → 인증됨/미인증 Badge, `connected_oauth` 유/무 표시
- 프로필 폼: name/birth/phone/소속/학교/학과/고민/관심사 prefill, 데모(`education:"재학"`)에서 학교/학과 노출 확인
- 저장 버튼 `disabled` + "곧 제공" 안내, 비밀번호 버튼 `disabled` + "(준비 중)" + has_password 라벨 분기
- 로그아웃 동작

- [ ] **Step 3: (선택) 스모크 결과 메모**

문제 발견 시 해당 Task 로 돌아가 수정 후 재검증.

---

## Self-Review (작성자 체크 완료)

- **Spec coverage**: 프로필 편집 폼(Task4)·플랜(Task2)·계정정보 email_verified/connected_oauth(Task1)·비밀번호 진입점 has_password 분기(Task3)·저장 비활성 피드백(Task4)·모바일/데스크톱 레이아웃(Task5) — 모든 DoD 매핑됨. 저장 성공/실패 토스트는 의도적으로 BAC-5 후속(범위 외, spec §2).
- **Placeholder scan**: 코드 내 `// TODO(BAC-5)`·`// TODO(FRT-8)` 는 의도된 후속 연동 시밍(주석)으로, dead call 아님. 그 외 미완성 표현 없음.
- **Type consistency**: `Account`/`Profile`(`@/types/auth`), `AffiliationStatus`(`@/app/(auth)/constants`), 컴포넌트 props 명 일관. `toAffiliation`·`toggle` 시그니처 일치. 모든 컴포넌트 named export, 페이지만 default export.
